import 'tailwindcss/tailwind.css';
import '../autocomplete.css'

// TODO: move layout into here and disable header for index
export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}
